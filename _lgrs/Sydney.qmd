---
title: Sydney
categories:
 - Australia
 - Familia
editor_options: 
  chunk_output_type: console
---

```{r}
#| warning: false
#| message: false

library(FlickrAPI)
library(pixture)
library(dplyr)
here::i_am("lgrs/Trieste.qmd")
file_name <- here::here("data","flickr-photos.rds")
all_photos <- readRDS(file=file_name)
htmlcode <- "<center>\n<img src='%s' width='%s'  height='%s'>\n<br/>\n**%s** by <a href='https://www.flickr.com/photos/%s/%s' target='flickr'>JR Ferrer Paris @ flickr</a><br/></center>"
```

```{r}
#| output: asis
seleccion <- c("Sydney")
photos <- all_photos %>% filter(grepl(seleccion,title) )
show_photo <- photos %>% 
    slice_sample(n=1) %>%
    transmute(code = sprintf(htmlcode, url_m, width_m, height_m, title, owner, id)) %>% pull()
cat(show_photo)
```


## Galería

```{r}
#| echo: false
if (nrow(photos)>3) {
  paths <- photos %>% pull(url_m)
  captions <- photos %>% 
    transmute(caption=sprintf("<strong>%s</strong> por <a href='https://www.flickr.com/photos/jferrer/%s' target='flickr'>JR Ferrer Paris @ flickr</a>",title,id)) %>% pull()
  pixgallery(paths, caption=captions)
}
```


<blockquote class="imgur-embed-pub" lang="en" data-id="AJYSnM6"  ><a href="//imgur.com/AJYSnM6">Colourful rockpool</a></blockquote><script async src="//s.imgur.com/min/embed.js" charset="utf-8"></script>


## Observaciones en iNaturalist

```{r}
#| echo: false
#| warning: false
#| message: false

library(pixture)
suppressMessages(library(dplyr))
suppressMessages(here::i_am("lgrs/Trieste.qmd"))
file_name <- here::here("data","iNaturalist-obs-NeoMapas.rds")
all_photos <- readRDS(file=file_name) 

photos <- all_photos 

```

```{r}
slc_photos <- all_photos %>% 
    filter(grepl("Sydney",place_guess)) %>% 
    slice_sample(n=27)

paths <- slc_photos %>% 
    pull(image_url)
captions <- slc_photos %>% 
    transmute(caption=sprintf("<strong>%s</strong> (<emph>%s</emph>) by <a href='%s' target='iNat'>NeoMapas @ iNaturalist</a>",species_guess,scientific_name,url)) %>% pull()

pixgallery(paths, caption=captions)
```